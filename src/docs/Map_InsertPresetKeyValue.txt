Desmontagem

Fluxo de controle


EMPURRAR
RBP
MOV
RBP
,
RSP
SUB
RSP
,
XOR
EAX
,
EAX
CMP
RSP
,
qword ptr [R14 + ]
JBE
MOV
R11
,
qword ptr [R15 + ]
MOV
qword ptr [RSP + ]
,
R11
MOV
qword ptr [RSP]
,
RAX
CHAMAR
FUN_003cda50
MOV
qword ptr [RBP + - ]
,
RAX
XOR
ESI
,
ESI
MOV
RBX
,
qword ptr [RBP + ]
MOV
EDX
,
MOV
qword ptr [RBP + - ]
,
RSI
CMP
RSP
,
qword ptr [R14 + ]
JBE
CMP
RSI
,
RBX
JGE
MOV
RCX
,
RSI
SHL
RCX
,
MOV
RDI
,
qword ptr [RBP + ]
CMP
RCX
,
JA
SAR
RDI
,
CL
MOV
EDI
,
EDI
E
EDI
,
EDX
MOV
qword ptr [RBP + - ]
,
RDI
MOV
RCX
,
qword ptr [RAX + ]
MOV
R8
,
qword ptr [RAX + ]
MOV
R9
,
qword ptr [R8 + ]
SAR
RCX
,
MOV
qword ptr [RBP + - ]
,
RCX
SAR
R9
,
CMP
RCX
,
R9
JNZ
MOV
qword ptr [RSP]
,
RAX
CHAMAR
FUN_003cd89c
MOV
RCX
,
qword ptr [RBP + - ]
Descompilação de IA





// Return type is a pointer to some Node structure.
NodePtr
FUN_00571c5c(
   void
)
{
   // Local variables: base_ptr points to context data, current_node tracks the linked list node being processed.
   NodePtr base_ptr, current_node;
   long index;
   unsigned int value;
   NodePtr result;
   // Extract a base pointer from global state context at offset 64.
   base_ptr = (*((NodePtr *) ((void *) ((unsigned long) (global_var1.UNK_FIELD_0) + 64))));
   // Initialize current_node, likely creating or fetching head of a linked list.
   current_node = FUN_003cda50();
   // Loop from 0 to node_count-1 (count stored at offset 64 from base_ptr).
   for (index = 0; index < ((long) (*((NodePtr *) ((void *) ((unsigned long) (base_ptr) + 64)))) - 1); index++) {
       // Read an 8-byte data value at offset index*8 from some data array.
       value = read_data((unsigned long) index << 3);
       // Check if current node's size/count is even (low bit clear).
       if (((long) (current_node)->header.size_or_count) == ((long) (((long) (((long) (current_node)->header.size_or_count) >> 1)) << 1))) {
           // Call helper function when size/count is even, likely a cleanup or reallocation routine.
           FUN_003cd89c((unsigned long) index << 3);
       }
       // Write transformed size value (shifted and tagged) into node payload at offset 15.
       (((long *) ((void *) ((unsigned long) (current_node) + 15)))[0]) = ((NodePtr) (((unsigned long) (((long) (((long) (current_node)->header.size_or_count) >> 1)) << 1) | 1) << 1));
       // Store the read data value (shifted and tagged) into node payload at offset 23.
       (((long *) ((void *) ((unsigned long) (current_node) + 15)))[1]) = ((NodePtr) (((unsigned long) (value) << 1) | 1));
       // Save current node as result before advancing.
       result = current_node;
       // Advance to next node in linked list.
       current_node = current_node->next.link;
   }
   // Return the last node processed (tail of list).
   return result;
}
Callees
2
Endereço	Nome
0x3cd89c	
FUN_003cd89c
0x3cda50	
FUN_003cda50
Chamadores
2
Endereço	Nome
0x570e00	
FUN_00570e00
0x571d98	
FUN_00571d98