Desmontagem

Fluxo de controle


EMPURRAR
RBP
MOV
RBP
,
RSP
SUB
RSP
,
CMP
RSP
,
qword ptr [R14 + ]
JBE
MOV
R11
,
qword ptr [R14 + ]
MOV
qword ptr [RSP + ]
,
R11
MOV
qword ptr [RSP]
,
CHAMAR
FUN_003d6308
MOV
RCX
,
qword ptr [RAX + ]
MOVSD
XMM0
,
qword ptr [RBP + ]
CVTSD2SS
XMM0
,
XMM0
MOV
RAX
,
qword ptr [RCX + ]
MOVSS
dword ptr [RAX]
,
XMM0
MOV
RAX
,
qword ptr [RCX + ]
MOVSXD
RCX
,
dword ptr [RAX]
MOVSXD
RCX
,
ECX
MOV
R11
,
qword ptr [RBP + ]
MOV
qword ptr [RSP + ]
,
R11
MOV
qword ptr [RSP + ]
,
RCX
MOV
EAX
,
MOV
qword ptr [RSP]
,
RAX
CHAMAR
FUN_00571c5c
MOV
RSP
,
RBP
POP
RBP
RET
CHAMAR
qword ptr [R14 + ]
JMP
Descompilação de IA





// Return type and function name indicating this operates on a context pointer structure
Context_ptr
FUN_00571d98(
   // Parameter: pointer to a context structure containing buffer state
   Context_ptr ctx
)
{
   Context_ptr result;
   Context_ptr new_buffer;
   double val_double;
   int val_int;
   Context_ptr field_offset;
   {
       Context_ptr buffer;
       {
           // Check if buffer has been exhausted and needs refilling
           if ((ctx)->buffer_state.end <= (ctx)->buffer_state.current) {
               // Call refill callback to get more data into the buffer
               (ctx)->buffer_state.refill_buffer(ctx);
           }
           // Parse/allocate a new buffer object from current position with type/size indicator 8
           buffer = FUN_003d6308(ctx, (ctx)->buffer_state.current, (ctx)->buffer_state.end, ((Context_ptr) 8));
           (new_buffer) = buffer;
       }
       // Calculate header pointer offset by subtracting -8 (i.e., adding 8) from header_ptr, likely getting base structure address
       (result) = ((Context_ptr) ((long) (((new_buffer)->data.header_ptr) - (long) -8)));
   };
   // Extract double value from the result structure's data union/value field
   val_double = (double) ((result)->data.value_double);
   // Store the double as a float at the header pointer location, truncating precision
   (*(float *) ((result)->data.header_ptr)) = val_double;
   // Read back the stored value as an integer (bitwise reinterpretation or truncated float bits)
   val_int = (*(int *) ((result)->data.header_ptr));
   // Field offset constant 4, likely indicating which struct member to access
   field_offset = ((Context_ptr) 4);
   // Tail call to FUN_00571c5c with context, field offset, extracted int value, and recomputed header pointer
   return FUN_00571c5c(ctx, field_offset, ((Context_ptr) ((long) (val_int))), ((Context_ptr) ((long) (((new_buffer)->data.header_ptr) - (long) -8))));
}
Callees
2
Endereço	Nome
0x3d6308	
FUN_003d6308
0x571c5c	
FUN_00571c5c
Chamadores
1
Endereço	Nome
0x570e00	
FUN_00570e00
